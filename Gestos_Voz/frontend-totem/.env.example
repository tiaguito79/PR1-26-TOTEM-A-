# URL del panel/API (Next.js)
VITE_API_URL=http://localhost:3000

# El cliente inicia sesión con las credenciales del tótem (usuario + contraseña)
# generadas al crear el dispositivo en el panel de administración.
